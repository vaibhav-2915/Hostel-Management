<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Hostel</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
        }

        .card {
            max-width: 600px;
            margin: auto;
            border-radius: 10px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.15);
        }

        .form-label {
            font-weight: 600;
            color: #333;
        }

        .form-control {
            border-radius: 6px;
        }

        .btn-primary {
            background-color: #007bff;
            border: none;
            padding: 10px 15px;
            font-weight: bold;
            border-radius: 6px;
        }

        .btn-primary:hover {
            background-color: #0056b3;
        }

        .btn-secondary {
            padding: 10px 15px;
            font-weight: bold;
            border-radius: 6px;
        }

        .navbar {
            background-color: #fff;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }

        .navbar-dark .nav-link {
            font-weight: 500;
        }
    </style>
</head>

<body>
    <nav class="navbar navbar-light bg-white shadow-sm p-3">
        <div class="container d-flex justify-content-between">
            <a class="navbar-brand h4 text-primary" href="/dashboard/admin">Hostel Management</a>
            <a href="/auth/logout" class="btn btn-danger">Logout</a>
        </div>
    </nav>

    <div class="bg-light">
        <nav class="navbar navbar-expand-lg navbar-dark bg-dark sticky-top">
            <div class="container">
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="collapse navbar-collapse" id="navbarNav">
                    <ul class="navbar-nav mx-auto">
                        <li class="nav-item"><a class="nav-link" href="/dashboard/admin">Dashboard</a></li>
                        <li class="nav-item"><a class="nav-link" href="/admin/mess-menu">Mess Menu</a></li>
                        <li class="nav-item"><a class="nav-link" href="/admin/feedbacks">Feedbacks</a></li>
                        <li class="nav-item"><a class="nav-link" href="/admin/notifications">Notifications</a></li>
                        <li class="nav-item"><a class="nav-link active" href="/listings">Hostel Listings</a></li>
                    </ul>
                </div>
            </div>
        </nav>

        <div class="container mt-5">
            <h2 class="text-center mb-4">Edit Hostel</h2>
            <div class="card shadow p-4">
                <form action="/listings/edit/<%= listing._id %>?_method=PUT" method="POST">
                    <div class="mb-3">
                        <label class="form-label">Hostel Name</label>
                        <input type="text" class="form-control" name="name" value="<%= listing.name %>" required>
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Room Type</label>
                        <input type="text" class="form-control" name="room_type" value="<%= listing.room_type %>"
                            required>
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Room Occupancy</label>
                        <input type="text" class="form-control" name="room_occupancy"
                            value="<%= listing.room_occupancy %>" required>
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Price (INR)</label>
                        <input type="number" class="form-control" name="price" value="<%= listing.price %>" required>
                    </div>
                    <div class="mb-3">
                        <label class="form-label">AC Room</label>
                        <div class="form-check">
                            <input type="radio" class="form-check-input" name="ac_rooms" value="yes"
                                <%=listing.ac_rooms==='yes' ? 'checked' : '' %>>
                            <label class="form-check-label">Yes</label>
                        </div>
                        <div class="form-check">
                            <input type="radio" class="form-check-input" name="ac_rooms" value="no"
                                <%=listing.ac_rooms==='no' ? 'checked' : '' %>>
                            <label class="form-check-label">No</label>
                        </div>
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Description</label>
                        <textarea class="form-control" name="description" rows="3"><%= listing.description %></textarea>
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Image URLs</label>
                        <input type="text" class="form-control" name="images"
                            value="<%= listing.images ? listing.images.join(', ') : '' %>"
                            placeholder="Enter comma-separated image URLs">
                    </div>
                    <div class="d-flex justify-content-between">
                        <button type="submit" class="btn btn-primary">Update Hostel</button>
                        <a href="/listings" class="btn btn-secondary">Cancel</a>
                    </div>
                </form>
            </div>
        </div>
    </div>

    <%- include("../../../include/footer.ejs") %>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>